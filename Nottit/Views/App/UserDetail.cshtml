<h2>{{user.UserName}}</h2>

<h3>Submissions</h3>
<div ng-repeat"link in user.Links">
    {{link.Title}}
</div>

<h3>Comments</h3>
<div ng-repeat"comment in user.Comments">
    <div>{{comment.Link.Title}}</div>
    <div>-- {{comment.Text}}</div>
</div>